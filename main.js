import './action';
// импорт внешней библиотеки, которая например установлена с помощью npm
import lib from 'external-lib-name'; // в отличии от импорта из файла пишется только название библиотеки

console.log(lib);